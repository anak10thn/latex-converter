version: '3.8'

services:
  latex-converter:
    build: .
    ports:
      - "50458:50458"
    volumes:
      - ./uploads:/usr/src/app/uploads
      - ./output:/usr/src/app/output
    restart: unless-stopped